<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/all_auctions"
            android:icon="@drawable/ic_menu_camera"
            android:title="All Auctions"/>
        <item
            android:id="@+id/add_auction"
            android:icon="@drawable/ic_menu_gallery"
            android:title="Add Auction"/>
        <item
            android:id="@+id/log_out"
            android:icon="@drawable/ic_menu_manage"
            android:title="Log out"/>
    </group>

    <item android:title="My Bids">
        <menu>
            <item
                android:id="@+id/active_bids"
                android:icon="@drawable/ic_menu_share"
                android:title="Active Bids"/>
            <item
                android:id="@+id/won_auctions"
                android:icon="@drawable/ic_menu_send"
                android:title="Won Auctions"/>
        </menu>
    </item>

</menu>
